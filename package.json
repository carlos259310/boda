{
  "name": "boda-root",
  "private": true,
  "scripts": {
    "build": "cd astro-wedding && npm install --no-audit --no-fund && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/ || true"
  }
}
